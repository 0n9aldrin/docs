---
title: 'List API Keys'
openapi: get /api/organizations/{org_id}/projects/{project_id}/api-keys
---

List all API keys for a project. Only returns active and recently expired keys (revoked keys are hidden).

<Note>
API keys are displayed with only their prefix visible for security. The full key is only shown once upon creation.
</Note>

## Authentication

This endpoint requires JWT authentication with at least Member role in the organization.

```bash
Authorization: Bearer YOUR_JWT_TOKEN
```

## Path Parameters

<ParamField path="org_id" type="string" required>
  The unique identifier of the organization
</ParamField>

<ParamField path="project_id" type="string" required>
  The unique identifier of the project
</ParamField>

## Response

<ResponseField name="apiKeys" type="array">
  Array of API key objects
  
  <Expandable title="API Key Object">
    <ResponseField name="id" type="string">
      Unique identifier for the API key
    </ResponseField>
    
    <ResponseField name="name" type="string">
      Human-readable name for the API key
    </ResponseField>
    
    <ResponseField name="keyPrefix" type="string">
      First 12 characters of the API key (e.g., "pm_live_abc1")
    </ResponseField>
    
    <ResponseField name="status" type="string">
      Status of the key: `active`, `expired`, or `revoked`
    </ResponseField>
    
    <ResponseField name="permissions" type="array">
      Array of permissions: `["read", "write"]`
    </ResponseField>
    
    <ResponseField name="createdAt" type="string">
      ISO 8601 timestamp of when the key was created
    </ResponseField>
    
    <ResponseField name="lastUsedAt" type="string">
      ISO 8601 timestamp of last usage (null if never used)
    </ResponseField>
    
    <ResponseField name="expiresAt" type="string">
      ISO 8601 timestamp of expiration (null if no expiration)
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET https://api.photomem.ai/api/organizations/{org_id}/projects/{project_id}/api-keys \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

```typescript TypeScript
const response = await fetch(
  `https://api.photomem.ai/api/organizations/${orgId}/projects/${projectId}/api-keys`,
  {
    headers: {
      'Authorization': `Bearer ${jwtToken}`
    }
  }
);

const data = await response.json();
console.log(data.apiKeys);
```

```python Python
import requests

response = requests.get(
    f"https://api.photomem.ai/api/organizations/{org_id}/projects/{project_id}/api-keys",
    headers={"Authorization": f"Bearer {jwt_token}"}
)

data = response.json()
print(data["apiKeys"])
```
</RequestExample>

<ResponseExample>
```json Response
{
  "apiKeys": [
    {
      "id": "key_abc123",
      "name": "Production API Key",
      "keyPrefix": "pm_live_abc1",
      "status": "active",
      "permissions": ["read", "write"],
      "createdAt": "2024-01-15T10:30:00Z",
      "lastUsedAt": "2024-01-20T14:22:00Z",
      "expiresAt": null
    },
    {
      "id": "key_def456",
      "name": "Development Key",
      "keyPrefix": "pm_test_def4",
      "status": "active",
      "permissions": ["read"],
      "createdAt": "2024-01-10T09:15:00Z",
      "lastUsedAt": null,
      "expiresAt": "2024-12-31T23:59:59Z"
    }
  ]
}
```
</ResponseExample>
