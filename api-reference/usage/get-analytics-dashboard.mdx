---
title: "Get Analytics Dashboard"
api: "GET /api/organizations/{org_id}/analytics"
description: "Get comprehensive analytics dashboard data"
---

## Overview

Returns comprehensive analytics including top tags, top users, daily statistics, and performance metrics for visualization in dashboards.

## Authentication

Requires valid Clerk session token. User must be a member of the organization.

## Path Parameters

<ParamField path="org_id" type="string" required>
  Organization identifier
</ParamField>

## Query Parameters

<ParamField query="startDate" type="string">
  Start date in ISO 8601 format (defaults to 30 days ago)
</ParamField>

<ParamField query="endDate" type="string">
  End date in ISO 8601 format (defaults to now)
</ParamField>

<ParamField query="projectId" type="string">
  Optional project ID to filter analytics
</ParamField>

## Response

<ResponseField name="dateRange" type="object">
  <Expandable title="Date Range">
    <ResponseField name="start" type="string">Start date ISO 8601</ResponseField>
    <ResponseField name="end" type="string">End date ISO 8601</ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="metrics" type="object">
  <Expandable title="Metrics">
    <ResponseField name="totalMemories" type="number">Total memories created</ResponseField>
    <ResponseField name="totalUsers" type="number">Unique users</ResponseField>
    <ResponseField name="totalApiCalls" type="number">Total API calls</ResponseField>
    <ResponseField name="averageResponseTime" type="string">Average response time</ResponseField>
    <ResponseField name="errorRate" type="number">Error rate percentage</ResponseField>
    <ResponseField name="topTags" type="array">
      Most used tags
      <Expandable title="Top Tag">
        <ResponseField name="tag" type="string">Tag name</ResponseField>
        <ResponseField name="count" type="number">Usage count</ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="topUsers" type="array">
      Most active users
      <Expandable title="Top User">
        <ResponseField name="userId" type="string">User ID</ResponseField>
        <ResponseField name="userName" type="string">User name</ResponseField>
        <ResponseField name="memoryCount" type="number">Memory count</ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dailyStats" type="array">
      Daily breakdown
      <Expandable title="Daily Stat">
        <ResponseField name="date" type="string">Date (YYYY-MM-DD)</ResponseField>
        <ResponseField name="apiCalls" type="number">API calls</ResponseField>
        <ResponseField name="memoriesCreated" type="number">Memories created</ResponseField>
        <ResponseField name="memoriesDeleted" type="number">Memories deleted</ResponseField>
        <ResponseField name="errors" type="number">Error count</ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET "https://api.photomem.io/api/organizations/{org_id}/analytics?startDate=2025-01-01T00:00:00Z&endDate=2025-01-31T23:59:59Z" \
  -H "Authorization: Bearer YOUR_CLERK_TOKEN"
```

```python Python
import requests
from datetime import datetime, timedelta

end_date = datetime.now()
start_date = end_date - timedelta(days=30)

response = requests.get(
    f"https://api.photomem.io/api/organizations/{org_id}/analytics",
    params={
        "startDate": start_date.isoformat(),
        "endDate": end_date.isoformat()
    },
    headers={"Authorization": f"Bearer {clerk_token}"}
)

data = response.json()
metrics = data["metrics"]

print(f"Total Memories: {metrics['totalMemories']:,}")
print(f"Top Tags: {[t['tag'] for t in metrics['topTags'][:5]]}")
```

</RequestExample>

<ResponseExample>

```json 200 Success
{
  "dateRange": {
    "start": "2025-01-01T00:00:00Z",
    "end": "2025-01-31T23:59:59Z"
  },
  "metrics": {
    "totalMemories": 12500,
    "totalUsers": 245,
    "totalApiCalls": 45678,
    "averageResponseTime": "125ms",
    "errorRate": 1.06,
    "topTags": [
      {"tag": "important", "count": 1250},
      {"tag": "work", "count": 980},
      {"tag": "personal", "count": 765},
      {"tag": "project-a", "count": 543},
      {"tag": "follow-up", "count": 432}
    ],
    "topUsers": [
      {"userId": "user_abc123", "userName": "User abc123", "memoryCount": 1250},
      {"userId": "user_def456", "userName": "User def456", "memoryCount": 890},
      {"userId": "user_ghi789", "userName": "User ghi789", "memoryCount": 678}
    ],
    "dailyStats": [
      {
        "date": "2025-01-01",
        "apiCalls": 1234,
        "memoriesCreated": 456,
        "memoriesDeleted": 12,
        "errors": 8
      },
      {
        "date": "2025-01-02",
        "apiCalls": 1567,
        "memoriesCreated": 523,
        "memoriesDeleted": 15,
        "errors": 10
      }
    ]
  }
}
```

</ResponseExample>

