---
title: "List Webhooks"
api: "GET /api/organizations/{org_id}/projects/{project_id}/webhooks"
description: "List all webhooks for a project"
---

## Overview

Returns all webhooks configured for a project, including statistics and delivery information.

## Authentication

Requires valid Clerk session token. User must be a member of the organization.

## Path Parameters

<ParamField path="org_id" type="string" required>Organization identifier</ParamField>
<ParamField path="project_id" type="string" required>Project identifier</ParamField>

## Response

<ResponseField name="webhooks" type="array">
  Array of webhook objects
  
  <Expandable title="Webhook Object">
    <ResponseField name="id" type="string">Webhook identifier</ResponseField>
    <ResponseField name="url" type="string">Webhook URL</ResponseField>
    <ResponseField name="events" type="array">Array of event types to trigger on</ResponseField>
    <ResponseField name="status" type="string">Status: "active" or "paused"</ResponseField>
    <ResponseField name="secret" type="string">Webhook signing secret</ResponseField>
    <ResponseField name="description" type="string">Optional description</ResponseField>
    <ResponseField name="createdAt" type="string">Creation timestamp</ResponseField>
    <ResponseField name="updatedAt" type="string">Last update timestamp</ResponseField>
    <ResponseField name="lastDeliveredAt" type="string">Last successful delivery (nullable)</ResponseField>
    <ResponseField name="statistics" type="object">
      Delivery statistics
      <Expandable title="Statistics">
        <ResponseField name="totalDeliveries" type="number">Total deliveries</ResponseField>
        <ResponseField name="successfulDeliveries" type="number">Successful deliveries</ResponseField>
        <ResponseField name="failedDeliveries" type="number">Failed deliveries</ResponseField>
        <ResponseField name="averageResponseTime" type="string">Average response time</ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET "https://api.photomem.io/api/organizations/{org_id}/projects/{project_id}/webhooks" \
  -H "Authorization: Bearer YOUR_CLERK_TOKEN"
```

```python Python
import requests

response = requests.get(
    f"https://api.photomem.io/api/organizations/{org_id}/projects/{project_id}/webhooks",
    headers={"Authorization": f"Bearer {clerk_token}"}
)

webhooks = response.json()["webhooks"]
for wh in webhooks:
    print(f"{wh['url']}: {wh['status']}")
    if wh['statistics']:
        print(f"  Success rate: {wh['statistics']['successfulDeliveries']}/{wh['statistics']['totalDeliveries']}")
```

</RequestExample>

<ResponseExample>

```json 200 Success
{
  "webhooks": [
    {
      "id": "hook_abc123xyz",
      "url": "https://api.example.com/webhooks/photomem",
      "events": ["memory.created", "memory.updated", "memory.deleted"],
      "status": "active",
      "secret": "whsec_abc123xyz...",
      "description": "Production webhook",
      "createdAt": "2025-01-01T10:00:00Z",
      "updatedAt": "2025-01-10T15:30:00Z",
      "lastDeliveredAt": "2025-01-15T14:25:00Z",
      "statistics": {
        "totalDeliveries": 1250,
        "successfulDeliveries": 1238,
        "failedDeliveries": 12,
        "averageResponseTime": "145ms"
      }
    }
  ]
}
```

</ResponseExample>

## Event Types

- `memory.created` - Fired when a memory is created
- `memory.updated` - Fired when a memory is updated
- `memory.deleted` - Fired when a memory is deleted
- `project.updated` - Fired when project settings change

