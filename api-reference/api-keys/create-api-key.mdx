---
title: "Create API Key"
api: "POST /api/organizations/{org_id}/projects/{project_id}/api-keys"
description: "Create a new API key for a project"
---

## Overview

Creates a new API key for the specified project. The full key is only shown once in the response and cannot be retrieved again.

<Warning>
  **Important:** Save the full API key immediately. It will not be shown again after this response.
</Warning>

## Authentication

Requires a valid Clerk session token. User must have `admin` or `owner` role in the organization.

## Path Parameters

<ParamField path="org_id" type="string" required>
  The unique identifier of the organization
</ParamField>

<ParamField path="project_id" type="string" required>
  The unique identifier of the project
</ParamField>

## Request Body

<ParamField body="name" type="string" required>
  Human-readable name for the API key (minimum 3 characters)
</ParamField>

<ParamField body="permissions" type="array" default='["read", "write"]'>
  Array of permissions to grant (e.g., ["read", "write"])
</ParamField>

<ParamField body="expiresAt" type="string">
  Optional expiration date in ISO 8601 format. If not provided, key never expires.
</ParamField>

## Response

<ResponseField name="apiKey" type="object">
  <Expandable title="API Key Object">
    <ResponseField name="id" type="string">
      Unique identifier for the API key
    </ResponseField>

    <ResponseField name="name" type="string">
      Human-readable name for the API key
    </ResponseField>

    <ResponseField name="key" type="string">
      **Full API key value** - only shown once!
    </ResponseField>

    <ResponseField name="keyPrefix" type="string">
      First 12 characters of the key
    </ResponseField>

    <ResponseField name="status" type="string">
      Status of the key (always "active" for new keys)
    </ResponseField>

    <ResponseField name="permissions" type="array">
      Array of granted permissions
    </ResponseField>

    <ResponseField name="createdAt" type="string">
      ISO 8601 timestamp of creation
    </ResponseField>

    <ResponseField name="expiresAt" type="string">
      ISO 8601 timestamp of expiration (nullable)
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST "https://api.photomem.io/api/organizations/{org_id}/projects/{project_id}/api-keys" \
  -H "Authorization: Bearer YOUR_CLERK_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Production API Key",
    "permissions": ["read", "write"],
    "expiresAt": null
  }'
```

```python Python
import requests

response = requests.post(
    f"https://api.photomem.io/api/organizations/{org_id}/projects/{project_id}/api-keys",
    headers={
        "Authorization": f"Bearer {clerk_token}",
        "Content-Type": "application/json"
    },
    json={
        "name": "Production API Key",
        "permissions": ["read", "write"],
        "expiresAt": None
    }
)

api_key = response.json()["apiKey"]
print(f"API Key created: {api_key['key']}")
print("⚠️  Save this key - it won't be shown again!")
```

```javascript JavaScript
const response = await fetch(
  `https://api.photomem.io/api/organizations/${orgId}/projects/${projectId}/api-keys`,
  {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${clerkToken}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      name: 'Production API Key',
      permissions: ['read', 'write'],
      expiresAt: null
    })
  }
);

const { apiKey } = await response.json();
console.log(`API Key: ${apiKey.key}`);
console.log('⚠️  Save this key - it won\'t be shown again!');
```

</RequestExample>

<ResponseExample>

```json 201 Created
{
  "apiKey": {
    "id": "key_abc123xyz",
    "name": "Production API Key",
    "key": "pm_live_abc123xyz789def456ghi012jkl345mno678pqr901stu234",
    "keyPrefix": "pm_live_abc1",
    "status": "active",
    "permissions": ["read", "write"],
    "createdAt": "2025-01-15T10:30:00Z",
    "expiresAt": null
  }
}
```

```json 400 Bad Request
{
  "error": {
    "code": "invalid_request",
    "message": "API key name must be at least 3 characters"
  }
}
```

```json 403 Forbidden
{
  "error": {
    "code": "forbidden",
    "message": "Admin or owner role required"
  }
}
```

</ResponseExample>

## Key Format

API keys follow this format:
- **Live keys:** `pm_live_[43 random characters]`
- **Test keys:** `pm_test_[43 random characters]`

The prefix indicates the environment and is used for quick identification.

## Security Best Practices

<AccordionGroup>
  <Accordion title="Store Securely">
    - Never commit API keys to version control
    - Use environment variables or secret management services
    - Rotate keys regularly (every 90 days recommended)
  </Accordion>

  <Accordion title="Use Appropriate Permissions">
    - Grant minimum necessary permissions
    - Use read-only keys for analytics dashboards
    - Reserve write permissions for backend services only
  </Accordion>

  <Accordion title="Set Expiration Dates">
    - Set expiration dates for temporary access
    - Review and rotate keys before expiration
    - Use short-lived keys for testing
  </Accordion>
</AccordionGroup>

